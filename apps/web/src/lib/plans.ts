/**
 * What a Business pays to be on the platform.
 *
 * Here rather than on the pricing page because the drawer names the lower one
 * too, and a price quoted in two places from two literals is a price that will
 * disagree with itself.
 *
 * ponytail: placeholders until the real ones are set. Shekels, per month.
 */
export const SOLO_PRICE = 49;
export const TEAM_PRICE = 89;
