// A business is a place someone can link to. The customer app reads the
// business out of the path itself, so this route is the same screen — only
// arrived at cold rather than by tapping a search result.
export { default } from "../../page.tsx";
